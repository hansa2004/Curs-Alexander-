<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <size
        android:width="40dp"
        android:height="40dp" />

    <!-- Базовая заливка: непрозрачная, чтобы круг всегда рисовался.
         Дальше мы перекрашиваем его через backgroundTintList программно. -->
    <solid android:color="@android:color/white" />

    <!-- Лёгкая обводка, чтобы круг не "терялся" на светлых фонах -->
    <stroke
        android:width="1dp"
        android:color="#1A000000" />

</shape>
